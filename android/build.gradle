plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.14'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'webscreenshots-api'
            version = '1.1.14'

            from components.java

            pom {
                name = 'Website Screenshot'
                description = 'Web Screenshots is a simple tool for capturing screenshots of web pages. It returns an image screenshot of the web page provided.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/webscreenshots-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/webscreenshots-api.git'
                    url = 'https://github.com/apiverve/webscreenshots-api'
                }
            }
        }
    }
}
